# PostgreSQL (Neon, Supabase, or any Postgres)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Auth
NEXTAUTH_SECRET="generate-a-strong-random-secret-here"
NEXTAUTH_URL="https://your-app.vercel.app"

# Production Admin (optional — used by seed script and /setup page)
# If set, only this email can create the admin account via /setup
# If not set, anyone can use /setup once (first admin wins)
ADMIN_EMAIL="tamara.prudente@fora.travel"
ADMIN_PASSWORD=""
ADMIN_NAME="Tamara Prudente"
